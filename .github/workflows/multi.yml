name: Multi Step Workflow

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo "Fetching Jira"

      - name: Step 2
        run: echo "Finding Incident"

      - name: Step 3
        run: echo "Generating Summary"
